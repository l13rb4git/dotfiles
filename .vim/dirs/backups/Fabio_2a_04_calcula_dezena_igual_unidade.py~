"""Atividade_Fabio02_Condicionais; Fabio_2a_04_calcula_dezena_igual_unidade.

Leia 1 (um) número de 2 (dois) dígitos, verifique e escreva se o
algarismo da dezena é igual ou diferente do algarismo da unidade.

"""


def main():
    mostrar_info_programa()

    numero = int(input("Digite o número: "))

    sao_iguais = digito_dezena_igual_unidade(numero)
    mostrar_resultado_igualdade(sao_iguais)


def mostrar_info_programa():
    """Mostra ao usuário informações sobre o funcionamento do programa. """
    print("Informe um número de 2 (dois) dígitos e será mostrado se o "
          "dígito das dezenas é igual ao das unidades.")


def digito_dezena_igual_unidade(numero):
    digito_dezena, digito_unidade = separar_dezena_unidade(numero)
    if digito_dezena == digito_unidade:
        return True
    return False


def separar_dezena_unidade(numero):
    dezenas = numero // 10
    unidades = numero % 10
    return dezenas, unidades


def mostrar_resultado_igualdade(sao_iguais):
    if sao_iguais == True:  # if sao_iguais:
        resultado = "iguais"
    else:
        resultado = "diferentes"

    print("O dígito das dezenas e das unidades são %s." % resultado)


if __name__ == '__main__':
    main()
