"""Lista_Fabio02_Condicionais	Fabio_2a_11_tres_opcoes

Leia quatro números (opção, num 1, num2, num3) e que escreva o valor de num1
se opção igual a 1; o valor de num2 se opção or igual a 2; e o valor de num3
se opção for igual a 3. Os únicos valores possíveis para a variável opção
são 1, 2 e 3.

"""


def main():
    mostrar_info_programa()

    opcao = get_opcao()

    if opcao_invalida(opcao):
        print("Opção inválida.")

    else:
        primeiro_numero = get_inteiro_ordenado('primeiro')
        segundo_numero = get_inteiro_ordenado('segundo')
        terceiro_numero = get_inteiro_ordenado('terceiro')
        mostrar_numero_escolhido(opcao, primeiro_numero,
                                 segundo_numero, terceiro_numero)


def get_inteiro_ordenado(posicao):
    """Solicita ao usuário o enésimo número requerido. """
    return int(input("Digite o {} número: ".format(posicao)))


def get_opcao():
    return int(input("Escolha um opção de 1 a 3: "))


def mostrar_info_programa():
    """Mostra ao usuário informações sobre o funcionamento do programa. """
    print("I.A que consegue descobrir qual números você digitou antes mesmo "
          "que sejam digitados, basta informar uma opção e os números. ")


def opcao_invalida(opcao):
    return opcao < 1 or opcao > 3


def mostrar_numero_escolhido(opcao, primeiro_numero,
                             segundo_numero, terceiro_numero):

    numero_escolhido = primeiro_numero
    if opcao == 2:
        numero_escolhido = segundo_numero

    elif opcao == 3:
        numero_escolhido = terceiro_numero

    print("Você digitou o número {}.".format(numero_escolhido))


if __name__ == '__main__':
    main()
