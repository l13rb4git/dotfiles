"""Lista_Fabio02_Condicionais	Fabio_2a_15_profesor_maior_salario

Leia a quantidade de horas aula dadas por dois professores e o valor por
hora recebido por cada um. Escreva na tela qual dos professores tem salário
total maior.

"""


def main():
    mostrar_info_programa()
    professor_um_horas = get_aula_lecionadas('primeiro')
    professor_dois_horas = get_aula_lecionadas('segundo')

    professor_um_valor = get_pagamento_hora('primeiro')
    professor_dois_valor = get_pagamento_hora('segundo')

    calcular_maior_pagamento(professor_um_horas, professor_dois_horas,
                             professor_um_valor, professor_dois_valor)


def get_aula_lecionadas(posicao):
    """Solicita ao usuário o número de horas lecionadas pelo enésimo professor. """
    return int(input("Digite o número de horas de aulas dadas pelo "
                     "%s professor: " % posicao))


def get_pagamento_hora(posicao):
    """Solicita ao usuário o valor hora recebido pelo enésimo professor. """
    return int(input("Digite o valor hora cobrado pelo "
                     "%s professor: " % posicao))


def mostrar_info_programa():
    """Mostra ao usuário informações sobre o funcionamento do programa. """
    print("Informe quantidade de horas de aulas lecionadas e o pagamento por "
          "hora de dois professores, será mostrado qual deles ganha mais.")


def calcular_maior_pagamento(professor_um_horas, professor_dois_horas,
                             professor_um_valor, professor_dois_valor):

    professor_um_pagamento = calcular_pagamento(professor_um_horas,
                                                professor_um_valor)
    professor_dois_pagamento = calcular_pagamento(professor_dois_horas,
                                                  professor_dois_valor)

    if professor_um_pagamento == professor_dois_pagamento:
        print("Ambos recebem o mesmo.")
    elif professor_um_pagamento > professor_dois_pagamento:
        print("O primeiro professor ganha mais.")
    else:
        print("O segundo professor ganha mais.")


def calcular_pagamento(horas_lecionadas, valor_hora):
    return horas_lecionadas * valor_hora


if __name__ == '__main__':
    main()
