"""Lista_Fabio02_Condicionais	Fabio_2a_21_arredonda_decimais

Realize arredondamentos de números utilizando a regra usual da matemática:
se a parte fracionaria for maior do que ou igual a 0,5, o numero é arredondado
para o inteiro imediatamente superior, caso contrario, é arredondado para o
inteiro imediatamente inferior.

"""


def main():
    mostrar_info_programa()
    numero_decimal = get_decimal()
    parte_decimal = separar_decimal(numero_decimal)
    parte_inteira = truncar_numero(numero_decimal)
    arredondar_numero(parte_inteira, parte_decimal)


def mostrar_info_programa():
    """Mostra ao usuário informações sobre o funcionamento do programa. """
    print("Informe um número decimal e ele será arredondado.")


def get_decimal():
    """Solicita a usuário um valor correspondente a um número decimal. """
    return float(input("Digite o número decimal: "))


def separar_decimal(numero_decimal):
    return (numero_decimal * 10) % 10


def truncar_numero(numero_decimal):
    # Cheguei a essa formula por tentativa e erro no Python Interpreter
    # e meus conhecimento em matemática não me permitem afirmar o porque
    # dela funcionar.
    return numero_decimal - (((numero_decimal * 10**100) % 10**100) / 10**100)


def arredondar_numero(parte_inteira, parte_decimal):
    if parte_decimal >= 5:
        numero_arredondado = parte_inteira + 1
    else:
        numero_arredondado = parte_inteira - 1

    print("O número arredondado é: {}".format(numero_arredondado))


if __name__ == '__main__':
    main()
