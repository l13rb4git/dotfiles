"""Lista_Fabio02_Condicionais	Fabio_2a_30_caracteristica_quatro_digitos

Existem números de 4 dígitos (entre 1000 e 9999) que obedecem à seguinte
característica: se dividirmos o número em dois números de dois dígitos, um
composto pela dezena e pela unidade, e outro pelo milhar e pela centena, se
somarmos estes dois novos números gerando um terceiro, o quadrado deste
terceiro número é exatamente o número original de quatro dígitos. Por exemplo:
2025 -> dividindo: 20 e 25 -> somando temos 45 -> 45 ^ 2 = 2025.

"""


def main():
    mostrar_info_programa()
    numero = int(input("Digite o número de 4 (quatro) dígitos: "))

    obedece_caracteristica = verifica_caracteristica(numero)
    mostrar_resultado(obedece_caracteristica)


def mostrar_info_programa():
    """Mostra ao usuário informações sobre o funcionamento do programa. """
    print("Informe um número para saber se o quadrado da soma entre dois "
          "números, um composto pelo milhar e centena, e o outro pelas "
          "dezenas e unidades, é igual ao o número inicial.")


def verifica_caracteristica(numero):
    milhar_centenas = numero // 100
    dezenas_unidades = numero % 100
    soma_digitos = milhar_centenas + dezenas_unidades
    soma_ao_quadrado = soma_digitos ** 2

    if soma_ao_quadrado == numero:
        return True
    return False


def mostrar_resultado(obedece_caracteristica):
    resultado = 'não é'
    if obedece_caracteristica == True:    # if obedece_caracteristica:
        resultado = 'é'

    print("O número %s uns dos que obedece à essa característica." % resultado)


if __name__ == '__main__':
    main()
