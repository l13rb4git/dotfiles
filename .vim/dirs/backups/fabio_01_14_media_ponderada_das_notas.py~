"""Lista_fabio01; fabio_01_14_media_ponderada_das_notas.

Leia 3 notas de um aluno e o peso de cada nota, calcule e escreva a
média ponderada.

"""

# Entrada
# Mostrando informacoes sobre o programa
print("Informe três notas e os pesos dessas notas "
      "para receber a media ponderada dela.")

# Receber as notas
primeira_nota = float(input("Digite a 1ª nota: "))
segunda_nota = float(input("Digite a 2ª nota: "))
terceira_nota = float(input("Digite a 3ª nota: "))

# Receber o peso das notas
peso_primeira_nota = int(input("Digite o peso da 1ª nota ({}): ".format(
                                                            primeira_nota)))
peso_segunda_nota = int(input("Digite o peso da 2ª nota ({}): ".format(
                                                            segunda_nota)))
peso_terceira_nota = int(input("Digite o peso da 3ª nota ({}): ".format(
                                                            terceira_nota)))

# Processamento
# Multiplicar cada nota por seu peso
primeira_nota_mlp_peso = primeira_nota * peso_primeira_nota
segunda_nota_mlp_peso = segunda_nota * peso_segunda_nota
terceira_nota_mlp_peso = terceira_nota * peso_terceira_nota

# Somar o resultado das multiplicações
somas_multiplicacoes = primeira_nota_mlp_peso \
                        + segunda_nota_mlp_peso \
                        + terceira_nota_mlp_peso

# Somar os pesos
soma_pesos = peso_primeira_nota + peso_segunda_nota + peso_terceira_nota

# Dividir a soma das multiplicações pelos pesos
media_ponderada_notas = somas_multiplicacoes / soma_pesos

# Saída
print("\nA média ponderada das notas é {}.".format(media_ponderada_notas))
