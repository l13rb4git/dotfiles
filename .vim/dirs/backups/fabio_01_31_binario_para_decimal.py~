"""Lista_fabio01; fabio_01_31_binario_para_decimal.py

Leia um número inteiro (4 dígitos binários), calcule e escreva o
equivalente na base decimal.
"""

# Entrada
# Mostrando informacoes sobre o programa
print("Informe um número binário de 4 dígitos e será mostrado o seu valor "
      "convertido para decimal.")

numero_binario = int(input("Digite o número binário: "))

# Processamento
# Divindindo os bits em variáveis
primeriro_bit = numero_binario // 1000

tres_ultimos_bits = numero_binario % 1000
segundo_bit = tres_ultimos_bits // 100

dois_ultimos_bits = tres_ultimos_bits % 100
terceiro_bit = dois_ultimos_bits // 10

quarto_bit = dois_ultimos_bits % 10

# Convertendo os bits para decimal
# bit * 2 ^ n
decimal_primeiro_bit = primeriro_bit * (2 ** 3)
decimal_segundo_bit = segundo_bit * (2 ** 2)
decimal_terceiro_bit = terceiro_bit * (2 ** 1)
decimal_quarto_bit = quarto_bit

numero_decimal = decimal_primeiro_bit  \
                + decimal_segundo_bit  \
                + decimal_terceiro_bit \
                + decimal_quarto_bit

# Saída
print("O número binário {0} é igual a {1} em decimal."
      .format(numero_binario, numero_decimal))
