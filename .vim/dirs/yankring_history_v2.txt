int,v
+,v
!,v
,V
if __name__ == "__main__":    main(),V
separar_decimais,v
",v
quantas centenas, dezenas e " "unidades ele tem,v
def mostrar_info_programa():    """Mostra ao usuário informações sobre o funcionamento do programa. """    print("Digite um número, será mostrado quantas centenas, dezenas e "          "unidades ele tem.\n"),V
str_unidades == "",v
    # if str_centenas != "":    #     mensagem = mensagem + str_centenas    # if str_dezenas != "":    #     mensagem = mensagem + ", " + str_dezenas,V
=,v
or,v
),v
 ,v
    print(str_centenas, juncao, str_dezenas, sep=''),V
str_unidades, ,v
    else:,V
return,v
, ,v
    ,V
    if str_centenas == "" and str_dezenas == "":,V
if str_centenas != "" and str_dezenas != "":,v
tempo,v
and str_unidades != "",v
    if ,V
    if str_centenas != "" and str_dezenas != "" and str_unidades != "":,V
centenas,v
entenas,v
str_centenas != "",v
'',v
ção,v
decidir_juncao(str_unidades, str_dezenas, str_centenas),v
str_unidades, str_dezenas, str_centenas,v
,V
:      # if not str_unidades:,v
unidades,v
f", {str_dezenas}",v
f"{str_centenas}",v
    else:        ck,V
!=,v
centenas ,v
    if str_centenas != "":        mensagem = mensagem + f"{str_centenas}",V
        ,V
'':,v
:,v
    if str_unidades == '':      # if not str_unidades:        return '.'    else:        if str_centenas != '' or str_dezenas != '':            return f" e {str_unidades}."        else:            return f"{str_unidades}.",V
def organizar_primeira_parte(str_unidades, str_dezenas, str_centenas):    if str_unidades == '':      # if not str_unidades:        return '.'    else:        if str_centenas != '' or str_dezenas != '':            return f" e {str_unidades}."        else:            return f"{str_unidades}.",V
_parte,v
segunda,v
def organizar_segunda_parte(str_unidades, str_dezenas, str_centenas):    if str_unidades == '':      # if not str_unidades:        return '.'    else:        if str_centenas != '' or str_dezenas != '':            return f" e {str_unidades}."        else:            return f"{str_unidades}.",V
str_unidades,                                                str_dezenas,                                                str_centenas,v
primeira,v
e,v
        seturn ,V
r,v
            return f" e {str_unidades}.",V
a,v
g,v
nome_agru,v
agrupamento,v
f"{valor} {agrupamento}",v
centenas != ',v
str_centenas != '',v
    11j,V
            return,V
    if unidades != 0:        if dezenas != 0 or centenas != 0:,V
modificar_se_plural,v
valor, agrupamento,v
    return,v
            eeturn,V
unidades, dezenas, centenas,v
    msg = ''    if centenas != 0:        msg = msg + str_centenas,V
    if dezenas,V
        str_centenas = modificar_se_plural(centenas, "centena"),V
    print(str_unidades, str_dezenas, str_centenas),V
s,v
unidade,v
    str_unidades = modificar_se_plural(unidades, "unidade"),V
        print(unidades, dezenas, centenas, sep='\n'),V
dezenas,v
def separar_dezenas(numero):    return (numero % 100) // 10,V
        centenas = separar_centenas(numero),V
def separar_unidades(numero):    return numero % 10,V
        unidades = separar_unidades(numero),V
<,v
as duas notas parciais do aluno para saber sua situação,v
def main():    mostrar_info_programa()    primeira_nota = get_nota("1ª")    segunda_nota = get_nota("2ª")    media_notas = calcular_media(primeira_nota, segunda_nota)    conceito_nota = calcular_conceito_nota(media_notas)    situacao_aluno = calcular_situacao_aluno(media_notas)    mostrar_situacao_aluno(media_notas, conceito_nota, situacao_aluno)def mostrar_info_programa():    """Mostra ao usuário informações sobre o funcionamento do programa. """    print("Digite as duas notas parciais do aluno para saber sua situação.\n")if __name__ == "__main__":    main(),V
adicionada.,V
02b10 - Notas Conceitos -,v
C,v
el,v
"O aluno foi %s." % situacao_aluno,v
media_notas, conceito_nota, situacao_aluno,v
situacao_aluno,v
media_notas,v
7.5,v
5,v
.,v
7,v
