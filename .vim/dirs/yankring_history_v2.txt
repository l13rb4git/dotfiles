subtrativc,v
o,v
numero,v
<,v
 ,v
void taboada(int numero){,V
adicao,v
void taboda_subtracao(int numero, int subtrativo){    if (subtrativo >= 1){        taboda_adicao(numero, subtrativo - 1);        if (subtrativo < numero){            int diff = numero - subtrativo;            printf("%d - %d = %d\n", subtrativo, numero, diff);        }    }},V
    taboda_adicao(numero, 10);,V
    taboda_subtracao(numero, 10);,V
, subtrativo,v
},v
-,v
+,v
,V
soma,v
parcela,v
parcsubtrativo,v
ela,v
void taboda_adicao(int numero, int parcela){    if (parcela >= 1){        taboda_adicao(numero, parcela - 1);        int soma = numero + parcela;        printf("%d + %d = %d\n", numero, parcela, soma);    }},V
void taboda_adicao(int numero, int parcela){    if (parcela >= 1){        taboda_adicao(numero, parcela - 1);        int soma = numero + parcela;        printf("%d + %d = %d\n", numero, parcela, soma);    }},V
void taboda_adicao(int numero, int parcela){    if (parcela >= 1){        taboda_adicao(numero, parcela - 1);        int soma = numero + parcela;        printf("%d + %d = %d\n", numero, parcela, soma);    }},V
    if (parcela >= 1)        taboda_adicao(numero, parcela - 1);,V
limite,v
%s,v
/,v
,V
    printf("ADIÇÃO\n");,V
    printf("%d\n", numero);,V
%d,v
n,v
\,v
    fazer_operações(numero, '+', 1);,V
void fazer_operacoes(int numero, char operador, int razao){    printf("%d%s = kk\n", );},V
f,v
    return 0;,V
_atual,v
    printf("Taboda do %d:\n", numero_atual);,V
    },V
    if (numero_atual >= 10){,V
 numero_atual, int final,v
 numero,v
inicio, final,v
i,v
),v
d,v
%,v
   int inicio = 1;   int final = 10;,V
aboda ,v
T,v
        return numero * fatorial(numero-1);,V
nicio,v
fatorial,v
int,v
   scanf("%i", &numero);,V
  printf("Digite o número: ");   int numero;,V
   printf("%d\n", fatorial(numero));,V
Informe um número e será mostrado o seu fatorial,v
#include <stdio.h> void info_programma(){    printf("Informe um número e será mostrado o seu fatorial.\n");}int fatorial(int numero){    if (numero > 1){        return numero * fatorial(numero-1);    }}int main(){   info_programma();    printf("Digite o número: ");   int numero;   scanf("%i", &numero);   printf("%d\n", fatorial(numero));    return 0;},V
#include <stdio.h> ,V
0,v
        printf("%d \n", numero);,V
;,v
fatorial(numero),v
void,v
inicio,v
 inicial,v
   printf("Digite o limite: ");   int limite;   scanf("%i", &limite);   printf("Digite a razão: ");   int razao;   scanf("%i", &razao);   progressao_geometrica(inicio, limite, razao);,V
void progressao_geometrica(int inicio, int limite, int razao){    if (inicio != 0 && razao != 0 && inicio * razao <= limite){        progressao_geometrica(inicio * razao, limite, razao);        printf("%d\n", inicio);    }},V
valor inicial, um final, e uma razão " "e veja a progressão geometrica que eles formam,v
valor inicial, um final, e uma razão " "e veja a progressão geometrica que eles formam.\n"),v
#include <stdio.h> void info_programma(){    printf("Informe um valor inicial, um final, e uma razão "           "e veja a progressão geometrica que eles formam.\n");}void progressao_geometrica(int inicio, int limite, int razao){    if (inicio != 0 && razao != 0 && inicio * razao <= limite){        progressao_geometrica(inicio * razao, limite, razao);        printf("%d\n", inicio);    }}int main(){   info_programma();    printf("Digite o número inicial: ");   int inicio;   scanf("%i", &inicio);   printf("Digite o limite: ");   int limite;   scanf("%i", &limite);   printf("Digite a razão: ");   int razao;   scanf("%i", &razao);   progressao_geometrica(inicio, limite, razao);    return 0;},V
{,v
aritmetrica,v
aritmétrica,v
#include <stdio.h> void info_programma(){    printf("Informe um valor inicial, um final, e uma razão "           "e veja a progressão aritmétrica que eles formam.\n");}void progressao_aritmetrica(int inicio, int limite, int razao){    if (inicio + razao <= limite)        progressao_aritmetrica(inicio + razao, limite, razao);        printf("%d\n", inicio);}int main(){   info_programma();    printf("Digite o número inicial: ");   int inicio;   scanf("%i", &inicio);   printf("Digite o limite: ");   int limite;   scanf("%i", &limite);   printf("Digite a razão: ");   int razao;   scanf("%i", &razao);   progressao_aritmetrica(inicio, limite, razao);    return 0;},V
>,v
print_pares_anteriores,v
   printf("Digite o limite: ");   int limite;   scanf("%i", &limite);,V
número inicial,v
   printf("Digite o número inicial: ");   int inicio;   scanf("%i", &inicio);,V
    if (eh_par(limite))        printf("%d ", limite);    /* if (limite > 1) */    /*     print_pares_anteriores(limite - 2); */    /*     printf("%d ", limite); */},V
void print_pares_anteriores(int limite){    if (limite > 1)        print_pares_anteriores(limite - 1);,V
int eh_par(int limite){    if (limite % 2 == 0)        return 1;    else        return 0;},V
.,v
um número será mostrado todos os outros pares anteriores a ele,v
#include <stdio.h> void info_programma(){    printf("Informe um número será mostrado todos os outros pares anteriores a ele.\n");}int eh_par(int limite){    if (limite % 2 == 0)        return 1;    else        return 0;}void print_pares_anteriores(int limite){    if (limite > 1)        print_pares_anteriores(limite - 1);    if (eh_par(limite))        printf("%d ", limite);    /* if (limite > 1) */    /*     print_pares_anteriores(limite - 2); */    /*     printf("%d ", limite); */}int main(){   info_programma();    printf("Digite o número: ");   int limite;   scanf("%i", &limite);   print_pares_anteriores(limite);    return 0;},V
    if (limite > 1)        print_pares_anteriores(limite - 2);        printf("%d ", limite);,V
=,v
1,v
        printf("%d ", limite);,V
    {,V
        if (eh_par(limite))            printf("%d ", limite);,V
   int limite = 10;,V
printf("%d ", limite);,v
        },V
        if(limite%2){,V
        printf("%d\n", limite);,V
    printf("%d\n", limite);,V
        else,V
